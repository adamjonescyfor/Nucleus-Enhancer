// Copy this file to config.js and fill in the real values.
// config.js is gitignored and must never be committed.
// The proxy URL is NOT secret — it is your Cloudflare Worker endpoint.
// Consumer Key and Secret are stored as Cloudflare secrets, never here.
var CYFOR_CONFIG = {
    oauthProxyUrl: 'https://cyfor-oauth-proxy.YOURNAME.workers.dev'
};
