# Salesforce OAuth configuration (real proxy URL / local overrides).
# Copy config.example.js to config.js. Anchored to the repo root so it does NOT
# also match content/config.js (a real source file).
/config.js

# Build artefacts and OS noise
*.crx
*.pem
*.zip
.DS_Store
Thumbs.db
node_modules/

# Wrangler local state (contains Cloudflare account id/email — must not be committed)
oauth-proxy/.wrangler/

# CYFOR MG22 Word templates (managed in Salesforce, not committed)
*.docx
